{{- if .Values.nodeLauncher.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kumquat-backend.serviceAccountName" . }}
  labels:
    {{- include "kumquat-backend.labels" . | nindent 4 }}
{{- end }}
